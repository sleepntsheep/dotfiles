- defaults:
    link:
      relink: true


- clean: ['~']

- link:
    ~/.tmux.conf.local: tmux.conf.local
    ~/.vimrc: vimrc
    ~/.config/nvim: nvim
    ~/.ncmpcpp: ncmpcpp
    ~/.config/fish: fish
    ~/.mpd: mpd

- shell:
    - [git submodule update --init --recursive, Installing submodules]
